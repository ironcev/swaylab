library;

pub fn play() -> () {
    let s = (0u64, 0u64);

    if let (_, x) = s {
        let _a = x + x;
    };

// if true {
//     let x = s.1;
//     let _a = core::ops::add(x, x);
// }
// else {
// }
}
