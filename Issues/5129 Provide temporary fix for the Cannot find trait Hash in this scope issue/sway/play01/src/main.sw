script;

use std::hash::sha256;
// use std::hash::Hash;

// use std::hash::*;

// use std::hash::Hash as MyHash;

// fn sha256<T>(_x: T) -> u64 { 0 }

fn main() {
    let _a = 0;
    let _x = sha256(123u8);
}
